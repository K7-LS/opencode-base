# [ПРОЕКТ]

Память проекта — в `LLM/` (развёрнута project-memory [ДАТА]).

Начало сессии: прочитать `LLM/AGENTS.md`, затем верх
`LLM/ЖУРНАЛ СЕССИЙ.md` и `LLM/STATUS.md`.
Пути в документах — относительные от этой папки.
